-if session[:usuario]
  -if session[:usuario].rol=="Profesor"
    %p=link_to 'Administrar eventos', eventos_path

%h2 Curso 2012/2013

-#= form_tag meses_path, method => :get do
-#  = label :meses, :mes, 'MES'
-#  = select :meses, :mes, ['Septiembre','Octubre','Noviembre','Diciembre','Enero','Febrero', 'Marzo', 'Abril', 'Mayo','Junio']

-#  = submit_tag 'Ver mes'

-#= button_to 'Ver mes', meses_path(
- @meses.each do |mes|
  %tr
    %br
    %td= mes.mes
    %td= link_to "Ver mes", mes_path(mes)

- if session[:usuario]
  %p=link_to 'Cerrar sesion', '/logout'
- else
  %p=link_to 'Registro', '/new',:class => "link_registro"
  %p=link_to 'Acceder', '/login'
