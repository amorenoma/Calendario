%table
	%tr
		%th Asignatura: 
		%th= @evento.asignatura 
	%tr
		%th Tipo: 
		%th= @evento.tipo
	%tr
		%th Fecha: 
		%th= @evento.fecha
	%tr
		%th Aula: 
		%th= @evento.aula
	%tr
		%th Dificultad: 
		%th= @evento.dificultad
	%tr
		%th Comentarios:
	-@comentariosevento.each do |comentarioevento|
		%tr 
			%th= comentarioevento.usuario 
			%th= comentarioevento.comentario


-if session[:usuario]
	-#por si se usa nuevo comentario:
	-session[:evento]=@evento
	-session[:idevento]=params[:id]
	=link_to 'Nuevo comentario', new_comentario_path
	-if session[:usuario].rol=="Profesor"
		=link_to 'Modificar', edit_evento_path(@evento)
		=link_to 'Eventos', eventos_path
		=link_to 'Administrar comentarios', comentarios_path
		%p= button_to "Borrar", evento_path(@evento), :method => :delete, :confirm => "¿Estas seguro?"
%td= link_to "Calendario", mes_path(@mes)
