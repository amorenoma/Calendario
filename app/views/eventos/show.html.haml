%table
	%tr
		%th Asignatura: 
		%th= @evento.asignatura 
	%tr
		%th Tipo: 
		%th= @evento.tipo
	%tr
		%th Fecha: 
		%th= @evento.fecha
	%tr
		%th Aula: 
		%th= @evento.aula
	%tr
		%th Dificultad: 
		%th= @evento.dificultad

%p= button_to "Borrar", evento_path(@evento), :method => :delete, :confirm => "¿Estas seguro?"

-if session[:usuario]
	-if session[:usuario].rol=="Profesor"
		=link_to 'Modificar', edit_evento_path(@evento)
		=link_to 'Eventos', eventos_path
%td= link_to "Calendario", mes_path(@mes)
