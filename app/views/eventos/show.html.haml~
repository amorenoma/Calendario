%table.mostrarevento
	
	%tr
		%th.title Asignatura: 
		%th= @evento.asignatura 
	%tr
		%th.title Tipo: 
		%th= @evento.tipo
	%tr
		%th.title Fecha: 
		%th= @evento.fecha
	%tr
		%th.title Aula: 
		%th= @evento.aula
	%tr
		%th.title Dificultad: 
		%th= @evento.dificultad
	%tr
		%th.title Comentarios:
	-@comentariosevento.each do |comentarioevento|
		%tr 
			%th= comentarioevento.usuario
			%th.comentario= comentarioevento.comentario	


-if session[:usuario]
	-#por si se usa nuevo comentario:
	-session[:evento]=@evento
	-session[:idevento]=params[:id]
	=button_to 'Nuevo comentario', new_comentario_path, :class => "buttons"
	-if session[:usuario].rol=="Profesor"
		= button_to 'Modificar', edit_evento_path(@evento), :class => "buttons"
		= button_to 'Eventos', eventos_path, :class => "buttons"
		= button_to 'Administrar comentarios', comentarios_path, :class => "buttons"
		= button_to "Borrar", evento_path(@evento),:method => :delete, :confirm => "¿Estas seguro?"
%p
= form_tag  mes_path(@mes), :method => :get do
	= submit_tag 'Calendario'
