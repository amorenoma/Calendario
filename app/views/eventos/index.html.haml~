%p
= form_tag  new_evento_path, :method => :get do
  = submit_tag 'Crear evento'

%table#admineventos
  %thead
    %tr
      %th Fecha
      %th Asignatura
  %tbody
    - @eventos.each do |evento|
      %tr
        %td= evento.fecha
        %td.modeventoasig= evento.asignatura
        %td= link_to "Ver evento", evento_path(evento)
        %td= link_to "Modificar evento", edit_evento_path(evento)
        %td= button_to "Borrar", evento_path(evento), :method => :delete, :confirm => "¿Estas seguro?"

= form_tag homepage_index_path, :method => :get do
  = submit_tag 'Cancelar'
